window {
    withPack { true }
    allowMoving { true }

    gridLayout {
        columns 1
    }

    textLabel {
        id 'GOD_S_VOICE'
        text "God's voice!"
        width 300
    }

    textLabel {
        id 'GLOBAL_TIME'
        text "Current time"
    }
}
