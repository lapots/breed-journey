window (title : '$type properties') {
    withClosureButton { false }

    row {
        column {
            selectBox (label : 'Properties') {
                items CoreControl.resources["properties"]["$type"].collect { it.name }
            }
        }
    }
}
