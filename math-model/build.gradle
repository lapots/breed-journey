apply plugin: 'clojure-min'

clojure {
    aotCompile = true
    warnOnReflection = true
}

repositories {
    jcenter()
    clojarsRepo()
}

dependencies {
    compile "org.clojure:clojure:$clojureVersion"
}
